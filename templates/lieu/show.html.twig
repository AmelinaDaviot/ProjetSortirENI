{% extends 'base.html.twig' %}

{% block title %}Lieu detail{% endblock %}

{% block body %}
    <h1>Location</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ location.id }}</td>
        </tr>
        <tr>
            <th>Name</th>
            <td>{{ location.name }}</td>
        </tr>
        <tr>
            <th>Address</th>
            <td>{{ location.address }}</td>
        </tr>

        </tbody>
    </table>

    <a href="{{ path('location_index') }}">retour a la liste</a>

    <a href="{{ path('location_edit', {'id': location.id}) }}">modifier</a>

    {{ include('location/_delete_form.html.twig') }}
{% endblock %}
