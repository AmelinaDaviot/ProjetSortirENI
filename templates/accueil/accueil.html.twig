{% extends 'base.html.twig' %}

    {% block title %}
        {{ parent() }}
    {% endblock %}

    {% block informations %}
        <h1 class="jumbotron-heading text-center">Sorties organisées</h1>
    {% endblock %}

    {% block main %}

        {{ include('accueil/messageFlash.html.twig') }}

        {% if app.user %}
            <h2 class="mb-4">Filtrer les sorties</h2>
            {% include 'sortie/filtre.html.twig' %}
        {% endif %}

        {% if app.user %}
            <a href="{{ path('sortie_creer') }}" type="button"
               class="btn btn-dark btn-lg mb-5 mt-4" style="width:100%">Créer une sortie</a>
        {% endif %}

        {{ include('sortie/listeSorties.html.twig') }}

    {% endblock %}
