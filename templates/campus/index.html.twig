{% extends 'base.html.twig' %}

{% block title %}Campus liste{% endblock %}

{% block body %}
    <h1>Campus index</h1>

    <table class="table">
        <thead>
        <tr>
            <th>Id</th>
            <th>Name</th>
            <th>actions</th>
        </tr>
        </thead>
        <tbody>
        {% for campus in campuses %}
            <tr>
                <td>{{ campus.id }}</td>
                <td>{{ campus.name }}</td>
                <td>
                    <a href="{{ path('campus_show', {'id': campus.id}) }}">apercu</a>
                    <a href="{{ path('campus_edit', {'id': campus.id}) }}">modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('campus_new') }}">Créer nouveau</a>
{% endblock %}
