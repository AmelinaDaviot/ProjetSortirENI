{% extends 'base.html.twig' %}

{% block title %}Campus detail{% endblock %}

{% block body %}
    <h1>Campus</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ campus.id }}</td>
        </tr>
        <tr>
            <th>Name</th>
            <td>{{ campus.name }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('campus_index') }}">retour a la liste</a>

    <a href="{{ path('campus_edit', {'id': campus.id}) }}">modifier</a>

    {{ include('campus/annuler_sortie.html.twig') }}
{% endblock %}
